# RepoPulse AI — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
Flask==3.1.1
Werkzeug==3.1.3

# Environment variable loading
python-dotenv==1.0.1

# HTTP client — used by GitHubService for all GitHub REST API calls
requests==2.32.3

# Google Gemini AI SDK
google-generativeai==0.8.3

# Production WSGI server (use instead of Flask dev server)
gunicorn==23.0.0
